version: '3.8'

services:
  pet_service_db:
    image: mysql:8.2
    container_name: pet_service_db
    command: --default-authentication-plugin=mysql_native_password
    environment:
      - MYSQL_ROOT_PASSWORD=root
      - MYSQL_DATABASE=pet_service_db
    ports:
      - "3306:3306"
    networks:
      - senior_project_network
    volumes:
      - .docker/setup.sql:/docker-entrypoint-initdb.d/setup.sql
      - pet_service_db-service:/var/lib/mysql

  pet_service:
    container_name: pet_service
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    networks:
      - senior_project_network

networks:
  senior_project_network:

volumes:
  pet_service_db-service:
    driver: local